services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-aineko}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-aineko}
      POSTGRES_DB: ${POSTGRES_DB:-aineko}
      TZ: UTC
      PGTZ: UTC
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-aineko}"]
      interval: 5s
      timeout: 3s
      retries: 10

  aineko:
    build: .
    restart: unless-stopped
    network_mode: host
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./data:/data
      - ~/Obsidian:/data/vault:ro
      - chroma-cache:/root/.cache/chroma
      - ~/.cache/uv:/root/.cache/uv
      - ~/.local/share/opencode:/opencode-auth
    env_file:
      - .env
    environment:
      TZ: UTC
      AINEKO_DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-aineko}:${POSTGRES_PASSWORD:-aineko}@127.0.0.1:5432/${POSTGRES_DB:-aineko}
    devices:
      - nvidia.com/gpu=all

volumes:
  chroma-cache:
  postgres-data:
